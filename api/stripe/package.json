{
  "name": "@la/stripe",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts --keep-vars --env prod",
    "deploy:staging": "wrangler deploy src/index.ts --keep-vars --env staging"
  },
  "prettier": {
    "semi": false
  },
  "dependencies": {
    "edgedb": "^1.4.1",
    "hono": "^3.11.7",
    "stripe": "^14.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "wrangler": "^3.19.0"
  }
}
